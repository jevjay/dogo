config:
  input:
    - command: hello-stdout
      desc: Say hello
      timeout: 10
      type: docker
      format: stdout
      image: hello-world
    - command: hello-json
      desc: Say hello
      timeout: 3
      type: docker
      format: json
      image: thomaspoignant/hello-world-rest-json
